{
  "name": "task-allocation-mern",
  "version": "1.0.0",
  "private": true,
  "description": "Admin login, agent management, file upload, and task distribution MERN assignment.",
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev -w server\" \"npm run dev -w client\"",
    "build": "npm run build -w client",
    "seed": "npm run seed -w server",
    "install:all": "npm install"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
